// EXEMPLO DO SLIDE

#include <stdlib.h>
#include <stdio.h>

main() {
    int cont = 0;

    while (cont < 3) {
        printf("Exibindo o valor de cont %d \n", cont);
        cont++;
    }
    system("pause");
}
